# Quantum Adaptive Self Attention
This quantum project replaces the final classical dot-product attention block with a Parameterized Quantum Circuit using data re-uploading and circular entanglement, implemented in PyTorch and PennyLane and trained locally via backpropagation on a simulated state vector over a synthetic time-series dataset.
